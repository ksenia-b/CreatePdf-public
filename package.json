{
  "name": "createpdf-public",
  "version": "1.0.0",
  "description": "Creating pdf from the link",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "jscs index.js && jshint index.js || exit 0"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "nodemailer": "^2.6.4",
    "phantom": "^3.0.2"
  },
  "devDependencies": {}
}
